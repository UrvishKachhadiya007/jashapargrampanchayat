import {Link} from 'react-router-dom';import {Hero} from '../components/Common';import {events,ui} from '../data/content';import {useLanguage,pick} from '../context/LanguageContext';
export default function Events(){const {language}=useLanguage(),t=ui[language];return <><Hero title={t.events} subtitle="Village celebrations and memories preserved in photos." image="./images/hero-events.webp"/><section className="container card-grid event-grid">{events.map(x=><Link className="event-card" to={`/events/${x.id}`} key={x.id}><img src={x.cover}/><div><span>{x.date}</span><h3>{pick(x.name,language)}</h3><p>{pick(x.desc,language)}</p><b>{t.viewGallery} →</b></div></Link>)}</section></>}
