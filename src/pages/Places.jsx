import {Link} from 'react-router-dom';import {Hero} from '../components/Common';import {places,ui} from '../data/content';import {useLanguage,pick} from '../context/LanguageContext';
export default function Places(){const {language}=useLanguage(),t=ui[language];return <><Hero title={t.places} subtitle="Discover historical, spiritual and natural places in our village." image="/images/hero-places.webp"/><section className="container card-grid place-grid">{places.map(x=><Link className="place-card" to={`/places/${x.id}`} key={x.id}><img src={x.cover}/><div><span className="tag">{pick(x.category,language)}</span><h3>{pick(x.name,language)}</h3><p>{pick(x.desc,language)}</p><b>{t.viewDetails} →</b></div></Link>)}</section></>}
